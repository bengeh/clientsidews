import React from 'react';
import styled from 'styled-components';

const Header = () => {
    return(
        <HeaderContainer>
            <HeaderText>WS App</HeaderText>
        </HeaderContainer>
    )
}

const HeaderContainer = styled.div``
const HeaderText = styled.h2``

export default Header;